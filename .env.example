# Server-side only — neither of these needs a NEXT_PUBLIC_ prefix.
# The database id is only ever read server-side, so don't ship it to the browser.
NOTION_TOKEN=secret_tokenobtainedfromnotionapisite
NOTION_DATABASE_ID=databaseidinnotionpublished